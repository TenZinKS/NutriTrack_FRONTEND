import 'package:flutter/material.dart';
import 'package:nutri_track/core/theme/app_pallete.dart';

class AppTheme {
  static final darkThemeMode = ThemeData.dark().copyWith(
    scaffoldBackgroundColor: AppPalette.darkBackground,
  );
}